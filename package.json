{
  "name": "playlist-monorepo",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "start:backend": "cd backend && npm install && npm start",
    "start:frontend": "cd mood && npm install && npm run dev",
    "build:frontend": "cd mood && npm install && npm run build"
  }
}
